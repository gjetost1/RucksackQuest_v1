const globalvars = {
  // 'scene_height': window.innerHeight * .7,
  // 'scene_width': window.innerWidth * .7,
  'scene_height': 720,
  'scene_width': 1280,
  'ctx': document.getElementById("root")
}

export default globalvars
