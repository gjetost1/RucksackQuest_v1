const eventEngine = (eventObj) => {
  let {
    x,
    y,
    blockSize,
    eventType,
    eventDirection,
    eventAreaShape,
    eventXDim,
    eventYDim,
    eventEffect,
    eventDuration,
    eventTimeout
  } = eventObj

}

export default eventEngine
